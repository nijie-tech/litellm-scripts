# 复制为 .env 并填写;.env 会被 lib.sh 自动加载。
# 切勿把 .env 提交到版本库(脚本目录已建议加入 .gitignore)。

# 网关地址(默认本机 4000)
LITELLM_BASE_URL=http://0.0.0.0:4000

# 管理总钥匙(general_settings.master_key,sk- 开头)
LITELLM_MASTER_KEY=sk-请填你的master_key
